---
'@ai-sdk/anthropic': patch
---

feat(anthropic): `containerUploads` provider option on tool messages emits `container_upload` blocks next to the tool results, so files produced by a tool can be made available in the code execution container.
