---
"@ai-sdk/anthropic": patch
---

feat(anthropic): support citations for documents returned in tool results, `search_result` tool outputs (with `search_result_location` citations), and pass document `context` through to citation sources
